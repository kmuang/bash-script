# bash-script
Bash is used to give commands to an operating system, such as copying files (cp), removing files (rm), or searching for text (grep). It is the default shell on many Linux distributions and macOS.
